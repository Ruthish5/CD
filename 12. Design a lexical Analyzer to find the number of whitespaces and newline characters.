%{
#include <stdio.h>
%}

%%

"+"     { printf("Addition Operator (+)\n"); }
"-"     { printf("Subtraction Operator (-)\n"); }
"*"     { printf("Multiplication Operator (*)\n"); }
"/"     { printf("Division Operator (/)\n"); }

.|\n    { /* Ignore other characters */ }

%%

int main() {
    printf("Enter input (operators):\n");
    yylex();
    return 0;
}

int yywrap() {
    return 1;
}
