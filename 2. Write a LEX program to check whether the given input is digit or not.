%{
#include<stdio.h>
%}

%%
^[0-9]$     { printf("It's a digit: %s\n", yytext); }
.|\n        { printf("Not a digit: %s\n", yytext); }
%%

int yywrap() { return 1; }

int main() {
    printf("Enter a single character:\n");
    yylex();
    return 0;
}
